
const isWinter = true;
const isBadWeather = false;
const wasCleaned = false;
const forceCleaning = true;

if ((isWinter && isBadWeather && !wasCleaned) || forceCleaning) {
  console.log('Start cleaning!');
}
